Secure Password Authentication (Python)
Features
Uses PBKDF2 + SHA-256
Random salt for each password
Safe password verification
Easy to extend for databases later


Why this is strong
 No plain-text passwords
 Salt prevents rainbow-table attacks
 100,000 iterations slow down brute force
 hmac.compare_digest() avoids timing attacks