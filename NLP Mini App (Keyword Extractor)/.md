Date: 03/04/2026

Description

A simple NLP application that extracts important keywords from text.

Code
import string

text = input("Enter text: ").lower()

for c in string.punctuation:
    text = text.replace(c, "")

words = text.split()

stopwords = ["is", "the", "and"]
keywords = [w for w in words if w not in stopwords]

print("Keywords:", keywords)